/**
 * PDF Service
 *
 * 📌 INTEGRAÇÃO FUTURA — Geração de Relatórios em PDF
 *
 * Este serviço será responsável por:
 *   - Gerar relatórios de inspeção em formato PDF
 *   - Incluir cabeçalho com dados da loja, data e inspetor
 *   - Listar todos os itens do checklist com status (OK/NOK)
 *   - Incluir observações e fotos (se aplicável)
 *
 * DEPENDÊNCIAS FUTURAS:
 *   - pdfkit  → Geração de PDF server-side
 *   - OU puppeteer → Renderizar HTML para PDF (mais flexível para layouts complexos)
 *
 * O PDF gerado será enviado via e-mail usando o msGraph.service.js
 */

// Placeholder — será implementado na próxima fase
module.exports = {
  // generateInspectionReport: async (inspectionData) => {},
};
